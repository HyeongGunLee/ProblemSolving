/*
 *  Map
 *      - size(), empty(), find(k), put(k,v), erase(k),
 *      erase(p), begin(), end()
 *      - List-based Implementation(DLL)
 *      - implement with HashTable
 *          - worst: O(n), usually O(1)
 *
 *
 */